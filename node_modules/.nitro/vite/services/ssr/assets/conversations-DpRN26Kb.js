import { o as PageHeader } from "./Primitives-CaxSyH9b.js";
import { t as ConversationTable } from "./live-chats-BZmg40bL.js";
import { t as AdminShell } from "./AdminShell-CON-VFuj.js";
import { i as SelectFilter, n as FilterBar, r as SearchFilter, t as ALL } from "./Filters-D3xjA51g.js";
import { n as conversationsQuery } from "./conversations-BmcuG-c7.js";
import { useMemo, useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
//#region src/routes/conversations.tsx?tsr-split=component
var RANGES = {
	"Last 24 hours": 864e5,
	"Last 7 days": 6048e5,
	"Last 30 days": 2592e6
};
function ConversationsPage() {
	const query = useQuery(conversationsQuery());
	const [search, setSearch] = useState("");
	const [status, setStatus] = useState(ALL);
	const [intent, setIntent] = useState(ALL);
	const [escalated, setEscalated] = useState(ALL);
	const [range, setRange] = useState(ALL);
	const rows = query.data ?? [];
	const intents = useMemo(() => [...new Set(rows.map((r) => r.intent).filter((v) => Boolean(v)))], [rows]);
	const statuses = useMemo(() => [...new Set(rows.map((r) => r.status))], [rows]);
	const filtered = useMemo(() => {
		const cutoff = range === "__all__" ? null : RANGES[range];
		return rows.filter((r) => {
			if (cutoff && Date.now() - new Date(r.created_at).getTime() > cutoff) return false;
			if (status !== "__all__" && r.status !== status) return false;
			if (intent !== "__all__" && r.intent !== intent) return false;
			if (escalated === "Escalated" && !r.escalated) return false;
			if (escalated === "Not escalated" && r.escalated) return false;
			if (search) {
				if (!`${r.conversation_id} ${r.customer_id ?? ""} ${r.case_id ?? ""} ${r.intent ?? ""}`.toLowerCase().includes(search)) return false;
			}
			return true;
		});
	}, [
		rows,
		search,
		status,
		intent,
		escalated,
		range
	]);
	return /* @__PURE__ */ jsx(AdminShell, { children: /* @__PURE__ */ jsxs("div", {
		className: "space-y-4 p-4 md:p-6",
		children: [
			/* @__PURE__ */ jsx(PageHeader, {
				title: "Conversations",
				subtitle: "Search and review conversations handled by the support assistant."
			}),
			/* @__PURE__ */ jsxs(FilterBar, { children: [
				/* @__PURE__ */ jsx(SearchFilter, {
					label: "Search",
					placeholder: "Conversation, customer or case ID…",
					onChange: setSearch
				}),
				/* @__PURE__ */ jsx(SelectFilter, {
					label: "Date range",
					value: range,
					onChange: setRange,
					options: Object.keys(RANGES),
					allLabel: "All time"
				}),
				/* @__PURE__ */ jsx(SelectFilter, {
					label: "Intent",
					value: intent,
					onChange: setIntent,
					options: intents
				}),
				/* @__PURE__ */ jsx(SelectFilter, {
					label: "Status",
					value: status,
					onChange: setStatus,
					options: statuses
				}),
				/* @__PURE__ */ jsx(SelectFilter, {
					label: "Escalated",
					value: escalated,
					onChange: setEscalated,
					options: ["Escalated", "Not escalated"]
				})
			] }),
			/* @__PURE__ */ jsx(ConversationTable, {
				rows: filtered,
				loading: query.isLoading,
				error: query.isError ? query.error.message : null,
				onRetry: () => query.refetch(),
				showCreated: true,
				emptyTitle: "No conversations found",
				emptyDescription: "Conversations recorded by the support workflows will appear here."
			})
		]
	}) });
}
//#endregion
export { ConversationsPage as component };
