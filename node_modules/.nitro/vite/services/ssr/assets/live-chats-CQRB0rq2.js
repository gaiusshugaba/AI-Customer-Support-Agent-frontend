import { a as shortId, i as humanize, n as formatRelative } from "./format-Dd0mwkMk.js";
import { a as Mono, c as TableShell, l as TableSkeleton, n as ErrorState, o as PageHeader, t as EmptyState, u as Th } from "./Primitives-CaxSyH9b.js";
import { t as StatusBadge } from "./StatusBadge-C60_2PoF.js";
import { t as Badge } from "./badge-CvJ3zOG3.js";
import { t as AdminShell } from "./AdminShell-CON-VFuj.js";
import { i as SelectFilter, n as FilterBar, r as SearchFilter, t as ALL } from "./Filters-D3xjA51g.js";
import { a as liveConversationsQuery } from "./conversations-BmcuG-c7.js";
import { useMemo, useState } from "react";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
//#region src/routes/live-chats.tsx?tsr-split=component
var TIME_WINDOWS = {
	"Last hour": 36e5,
	"Last 24 hours": 864e5,
	"Last 7 days": 6048e5
};
function LiveChatsPage() {
	const query = useQuery(liveConversationsQuery());
	const [search, setSearch] = useState("");
	const [status, setStatus] = useState(ALL);
	const [intent, setIntent] = useState(ALL);
	const [escalation, setEscalation] = useState(ALL);
	const [window, setWindow] = useState("Last 24 hours");
	const rows = query.data ?? [];
	const intents = useMemo(() => [...new Set(rows.map((r) => r.intent).filter((v) => Boolean(v)))], [rows]);
	const statuses = useMemo(() => [...new Set(rows.map((r) => r.status))], [rows]);
	const filtered = useMemo(() => {
		const cutoff = TIME_WINDOWS[window];
		return rows.filter((r) => {
			if (cutoff && Date.now() - new Date(r.last_activity).getTime() > cutoff) return false;
			if (status !== "__all__" && r.status !== status) return false;
			if (intent !== "__all__" && r.intent !== intent) return false;
			if (escalation === "Escalated" && !r.escalated) return false;
			if (escalation === "Not escalated" && r.escalated) return false;
			if (search) {
				if (!`${r.conversation_id} ${r.customer_id ?? ""} ${r.intent ?? ""}`.toLowerCase().includes(search)) return false;
			}
			return true;
		});
	}, [
		rows,
		search,
		status,
		intent,
		escalation,
		window
	]);
	return /* @__PURE__ */ jsx(AdminShell, { children: /* @__PURE__ */ jsxs("div", {
		className: "space-y-4 p-4 md:p-6",
		children: [
			/* @__PURE__ */ jsx(PageHeader, {
				title: "Live Chats",
				subtitle: "Monitor conversations currently being handled by the AI support assistant."
			}),
			/* @__PURE__ */ jsxs(FilterBar, { children: [
				/* @__PURE__ */ jsx(SearchFilter, {
					label: "Customer / conversation",
					placeholder: "Search by ID or intent…",
					onChange: setSearch
				}),
				/* @__PURE__ */ jsx(SelectFilter, {
					label: "Status",
					value: status,
					onChange: setStatus,
					options: statuses
				}),
				/* @__PURE__ */ jsx(SelectFilter, {
					label: "Intent",
					value: intent,
					onChange: setIntent,
					options: intents
				}),
				/* @__PURE__ */ jsx(SelectFilter, {
					label: "Escalation",
					value: escalation,
					onChange: setEscalation,
					options: ["Escalated", "Not escalated"]
				}),
				/* @__PURE__ */ jsx(SelectFilter, {
					label: "Time",
					value: window,
					onChange: setWindow,
					options: Object.keys(TIME_WINDOWS),
					allLabel: "Any time"
				})
			] }),
			/* @__PURE__ */ jsx(ConversationTable, {
				rows: filtered,
				loading: query.isLoading,
				error: query.isError ? query.error.message : null,
				onRetry: () => query.refetch(),
				emptyTitle: "No live conversations in this window",
				emptyDescription: "Conversations appear here as soon as the AI support workflow records a turn."
			})
		]
	}) });
}
function ConversationTable({ rows, loading, error, onRetry, emptyTitle, emptyDescription, showCreated }) {
	if (error) return /* @__PURE__ */ jsx(TableShell, { children: /* @__PURE__ */ jsx("tbody", { children: /* @__PURE__ */ jsx("tr", { children: /* @__PURE__ */ jsx("td", { children: /* @__PURE__ */ jsx(ErrorState, {
		message: error,
		onRetry
	}) }) }) }) });
	return /* @__PURE__ */ jsxs(TableShell, { children: [/* @__PURE__ */ jsx("thead", {
		className: "bg-muted/50",
		children: /* @__PURE__ */ jsxs("tr", { children: [
			/* @__PURE__ */ jsx(Th, { children: "Customer" }),
			/* @__PURE__ */ jsx(Th, { children: "Conversation" }),
			/* @__PURE__ */ jsx(Th, { children: "Intent" }),
			/* @__PURE__ */ jsx(Th, { children: "Status" }),
			/* @__PURE__ */ jsx(Th, { children: "Last activity" }),
			showCreated && /* @__PURE__ */ jsx(Th, { children: "Created" }),
			/* @__PURE__ */ jsx(Th, { children: "Escalation" })
		] })
	}), /* @__PURE__ */ jsx("tbody", { children: loading ? /* @__PURE__ */ jsx(TableSkeleton, { cols: showCreated ? 7 : 6 }) : rows.length === 0 ? /* @__PURE__ */ jsx("tr", {
		className: "border-t border-border",
		children: /* @__PURE__ */ jsx("td", {
			colSpan: showCreated ? 7 : 6,
			children: /* @__PURE__ */ jsx(EmptyState, {
				title: emptyTitle,
				description: emptyDescription
			})
		})
	}) : rows.map((r) => /* @__PURE__ */ jsxs("tr", {
		className: "border-t border-border hover:bg-accent/40",
		children: [
			/* @__PURE__ */ jsx("td", {
				className: "px-4 py-2.5",
				children: r.customer_id ? /* @__PURE__ */ jsx(Mono, { children: shortId(r.customer_id, 12) }) : "—"
			}),
			/* @__PURE__ */ jsxs("td", {
				className: "px-4 py-2.5",
				children: [/* @__PURE__ */ jsx(Link, {
					to: "/conversations/$conversationId",
					params: { conversationId: r.conversation_id },
					className: "font-mono text-xs text-primary hover:underline",
					children: shortId(r.conversation_id, 12)
				}), /* @__PURE__ */ jsxs("span", {
					className: "ml-2 text-xs text-muted-foreground",
					children: [r.turn_count, " turns"]
				})]
			}),
			/* @__PURE__ */ jsx("td", {
				className: "px-4 py-2.5",
				children: humanize(r.intent)
			}),
			/* @__PURE__ */ jsx("td", {
				className: "px-4 py-2.5",
				children: /* @__PURE__ */ jsx(StatusBadge, { status: r.status })
			}),
			/* @__PURE__ */ jsx("td", {
				className: "px-4 py-2.5 whitespace-nowrap text-muted-foreground",
				children: formatRelative(r.last_activity)
			}),
			showCreated && /* @__PURE__ */ jsx("td", {
				className: "px-4 py-2.5 whitespace-nowrap text-muted-foreground",
				children: formatRelative(r.created_at)
			}),
			/* @__PURE__ */ jsx("td", {
				className: "px-4 py-2.5",
				children: r.case_id ? /* @__PURE__ */ jsx(Link, {
					to: "/escalations/$caseId",
					params: { caseId: r.case_id },
					className: "inline-flex",
					children: /* @__PURE__ */ jsxs(Badge, {
						variant: "outline",
						className: "cursor-pointer",
						children: ["Case ", shortId(r.case_id)]
					})
				}) : /* @__PURE__ */ jsx("span", {
					className: "text-xs text-muted-foreground",
					children: "No case"
				})
			})
		]
	}, r.conversation_id)) })] });
}
//#endregion
export { ConversationTable, LiveChatsPage as component };
